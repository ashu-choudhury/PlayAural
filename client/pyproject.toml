[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "PlaySonic-client"
version = "1.0.0"
description = "PlaySonic Game Client"
readme = "../README.md"
requires-python = ">=3.11"
dependencies = [
    "wxPython>=4.2.0",
    "accessible-output2>=0.16",
    "websockets>=10.0",
    "sound-lib>=0.2.2",
    "fluent-runtime>=0.4.0",
    "requests",
    "psutil",
    "keyring>=24.0",
    "livekit>=1.1.5",
    "sounddevice>=0.5.5",
]

[project.scripts]
PlaySonic-client = "client:main"

[tool.hatch.build.targets.wheel]
packages = ["."]

